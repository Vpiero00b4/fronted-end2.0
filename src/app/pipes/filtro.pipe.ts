// import { Pipe, PipeTransform } from '@angular/core';
// import { LibroResponse } from '../models/libro-response.models';

// @Pipe({
//   name: 'filtroPipe'
// })
//   export class FiltroPipe implements PipeTransform {
//     transform(libros: any[], searchTerm: string): any[] {
//       if (!libros || !searchTerm) {
//         return libros;
//       }
  
//       return libros.filter(libro => libro.titulo.toLowerCase().includes(searchTerm.toLowerCase()));
//     }
//   }
